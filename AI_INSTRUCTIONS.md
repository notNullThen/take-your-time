# AI Agent Instructions

**⚠️ CRITICAL: BEFORE MODIFYING ANY CODE IN THIS REPOSITORY ⚠️**

1. You **MUST** read all markdown files located inside the `AICache/` directory.
2. The `AICache/` contains heavily distilled, critical context about the application's non-standard architecture, including:
   - Client-side only persistence logic.
   - The cyclic (year-less) date and expiration algorithms.
   - Strict styling guidelines (flat glassmorphism, no box-shadows, vanilla CSS).
3. Do not attempt to add standard calendar libraries, server-side code, or heavy utility CSS frameworks (like Tailwind) without explicit user permission.

*Once you have ingested the `AICache/` files, you may proceed with the user's request.*
